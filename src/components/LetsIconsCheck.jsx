import React from "react";

export const LetsIconsCheck = ({ className }) => {
  return (
    <img
      className={`absolute top-0 left-0 w-3 h-3 ${className}`}
      alt="Lets icons check"
    />
  );
};
