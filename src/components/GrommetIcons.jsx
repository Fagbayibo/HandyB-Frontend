import React from "react";

export const GrommetIcons = ({ className }) => {
  return (
    <img
      className={`absolute top-[-2693px] left-[-2989px] w-7 h-7 ${className}`}
      alt="Grommet icons"
    />
  );
};
